@page "/"
@* @rendermode InteractiveServer *@

<style>
    body {
        background-color: blanchedalmond;

        h1 {
            text-align: center;
        }

        .content {
            padding: 5vw;

            background-color: bisque;
            border-radius: 10vw;

            h2 {
                text-align: center;
            }

            .newHolder {
                position: absolute;
                top: 50%;
                right: 50%;
                text-align: center;

            }

            #createNewUser {
                text-align: center;
                position: absolute;
                background-color: rgba(31, 31, 155, 0.494);
                padding: 15px;
                border-radius: 10px;
                top: 10px;
                right: 10px;
            }

        }
    }
</style>


<h1>Hello,WELCOME to CCN ATM service!</h1>

<div class="content">
    @if (userCounter == 0)
    {
        <h2>No USERS Available </h2>
    }
    else
    {
        <h2>USERS Available : @userCounter </h2>
    }

    <div class="addUser">
        <button id="createNewUser" @onclick="() => isClicked = true">Create new Account</button>
        @if (isClicked == true)
        {
            <div class="newHolder">
                <input type="text" placeholder="Enter your Account name/User name" @bind="userId">
                <input type="password" placeholder="Enter your new password" @bind="userPassword">
                <input type="password" placeholder="conform your new password" @bind="userPasswordConf">
                <button type="submit" class="summit" @onclick="createNewUser">SUMMIT</button>
            </div>
        }
    </div>

    <ul>
        @foreach (var i in listUser)
        {
            <li class="showUsers"> @i.UserId</li>
        }
    </ul>

</div>



@code {
    private static string userId = string.Empty;
    private static decimal initialAmount = 10;
    private string userPassword = string.Empty;
    private string userPasswordConf = string.Empty;

    private static int userCounter = 0;
    bool isClicked = false;
    private static List<OnlineATM.Index> listUser = new List<OnlineATM.Index>();

    private void createNewUser()
    {
        if (userPassword == userPasswordConf)
        {
            ++userCounter;
            listUser.Add(new OnlineATM.Index(userId, userPassword, initialAmount));
            isClicked = false;
            ClearForm();
        }
    }
    private void ClearForm()
    {
        userId = string.Empty;
        userPassword = string.Empty;
        userPasswordConf = string.Empty;
    }

}